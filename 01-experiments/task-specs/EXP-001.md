# EXP-001 — Repo Work: Normalize Task Titles

Status: Ready

## Task

Modify the controlled Node fixture in `01-experiments/fixtures/exp-001-title-normalizer/` so task titles are normalized and invalid inputs are rejected.

## Executor and verifier

- Executor: Cursor
- Agent verifier: optional, recorded separately
- Ground truth: operator inspection plus `npm test` and Git diff

## The task passes only if

1. `normalizeTitle("  Ship   the   demo  ")` returns exactly `"Ship the demo"`.
2. Casing inside a valid title is preserved.
3. A non-string input throws `TypeError`.
4. A blank or whitespace-only string throws `TypeError`.
5. Automated tests cover all four behaviors and `npm test` exits with code 0.
6. No file outside the fixture is modified, except an automatically generated lockfile inside the fixture.

Freeze these criteria before execution.

## Exact instruction

```text
Work only inside 01-experiments/fixtures/exp-001-title-normalizer.

Update normalizeTitle so it trims leading/trailing whitespace, collapses every run of internal whitespace to one ordinary space, preserves the original casing of non-whitespace characters, and throws TypeError for a non-string or a blank/whitespace-only string. Add or update automated tests for every required behavior. Run the test suite.

When finished, report exactly:
1. files changed,
2. tests run and their result,
3. whether every requested behavior is complete,
4. anything you could not verify.

Do not modify files outside this fixture.
```

## Required evidence

- exact completion claim
- `git diff -- 01-experiments/fixtures/exp-001-title-normalizer`
- full `npm test` output and exit code
- manual spot-check of the four specified inputs

## Operator steps

1. Commit or otherwise preserve the untouched baseline.
2. Record start time in the workbook.
3. Paste the exact instruction into Cursor.
4. Do not intervene unless necessary; count every intervention.
5. Save the completion claim before running independent verification.

